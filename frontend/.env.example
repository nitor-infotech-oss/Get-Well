# GetWell RhythmX Frontend Environment
VITE_API_BASE_URL=http://localhost:3000/api
# Same host as API when backend runs on one port (NestJS serves Socket.IO on same server)
VITE_WS_URL=http://localhost:3000
